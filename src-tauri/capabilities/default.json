{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core + event（Phase 1: pty:// イベントの listen/emit 用）。Phase 2+ で webview 生成 / updater を追加。",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
