{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core 権限のみ（Phase 0）。Phase 1+ で event / webview 生成 / updater を追加。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
